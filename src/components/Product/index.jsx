import React from "react";
import ProductCard from "./ProductCard";

const Products = () => {
  return (
    <React.Fragment>
      <ProductCard />
    </React.Fragment>
  );
};

export default Products;
